module PhiVty.Data.UI (
                      UIState(..),
                      CollectionType(..),
                      ) where

data UIState = UINormal | UISEdit

data CollectionType = CTNormal | CTMenu
